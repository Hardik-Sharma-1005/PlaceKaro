{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "studentProfiles": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "skills": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ],
      "$id": {
        ".read": "auth != null && data.child('studentId').val() === auth.uid",
        ".write": "auth != null && (!data.exists() || data.child('studentId').val() === auth.uid) && (!newData.exists() || newData.child('studentId').val() === auth.uid)"
      }
    },

    "projects": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ],
      "$id": {
        ".read": "auth != null && data.child('studentId').val() === auth.uid",
        ".write": "auth != null && (!data.exists() || data.child('studentId').val() === auth.uid) && (!newData.exists() || newData.child('studentId').val() === auth.uid)"
      }
    },

    "internships": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ],
      "$id": {
        ".read": "auth != null && data.child('studentId').val() === auth.uid",
        ".write": "auth != null && (!data.exists() || data.child('studentId').val() === auth.uid) && (!newData.exists() || newData.child('studentId').val() === auth.uid)"
      }
    },

    "certifications": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ],
      "$id": {
        ".read": "auth != null && data.child('studentId').val() === auth.uid",
        ".write": "auth != null && (!data.exists() || data.child('studentId').val() === auth.uid) && (!newData.exists() || newData.child('studentId').val() === auth.uid)"
      }
    },

    "achievements": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ],
      "$id": {
        ".read": "auth != null && data.child('studentId').val() === auth.uid",
        ".write": "auth != null && (!data.exists() || data.child('studentId').val() === auth.uid) && (!newData.exists() || newData.child('studentId').val() === auth.uid)"
      }
    },

    "evidence": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ],
      "$id": {
        ".read": "auth != null && data.child('studentId').val() === auth.uid",
        ".write": "auth != null && (!data.exists() || data.child('studentId').val() === auth.uid) && (!newData.exists() || newData.child('studentId').val() === auth.uid)"
      }
    },

    "jobs": {
      ".read": "auth != null && query.orderByChild === 'status' && query.equalTo === 'published'",
      ".indexOn": [
        "status"
      ]
    },

    "applications": {
      ".read": "auth != null && query.orderByChild === 'studentId' && query.equalTo === auth.uid",
      ".indexOn": [
        "studentId"
      ]
    },

    "notifications": {
      ".read": "auth != null && query.orderByChild === 'userId' && query.equalTo === auth.uid",
      ".indexOn": [
        "userId"
      ]
    }
  }
}